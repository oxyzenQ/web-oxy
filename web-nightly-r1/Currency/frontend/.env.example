# Development mode - uses localhost backend
MODE=development
ITE_API_BASE_URL=http://localhost:8000

# Production mode - uses relative API path (configured by deployment)
#MODE=production
# VITE_API_BASE_URL=/api

# Note: In production, the API URL should be configured by your deployment platform
# or reverse proxy. Do not hardcode external URLs in environment files for security.
